import { Button } from 'component-lib';

function App() {
  return (
    <div className="App">
      <Button>Tsup lib button</Button>
      {/* <Time date={new Date()} /> */}
    </div>
  );
}

export default App;
